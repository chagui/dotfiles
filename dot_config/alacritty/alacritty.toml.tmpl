[general]
import = ["~/.config/alacritty/catppuccin-macchiato.toml", "~/.config/alacritty/key_bindings.toml"]
live_config_reload = true

[env]
TERM = "xterm-256color"

[font]
size = 12

[font.bold]
family = "JetBrainsMono Nerd Font"
style = "Bold"

[font.bold_italic]
family = "JetBrainsMono Nerd Font"
style = "Bold Italic"

[font.italic]
family = "JetBrainsMono Nerd Font"
style = "Italic"

[font.normal]
family = "JetBrainsMono Nerd Font"
style = "Regular"

[[hints.enabled]]
{{- if eq .chezmoi.os "darwin" }}
command = "open"
{{- else if eq .chezmoi.os "linux" -}}
{{-   if eq .chezmoi.osRelease.id "ubuntu" }}
command = "xdg-open"
{{-   end }}
{{- end }}
hyperlinks = true
post_processing = true
regex = "(ipfs:|ipns:|magnet:|mailto:|gemini:|gopher:|https:|http:|news:|file:|git:|ssh:|ftp:)[^\u0000-\u001F\u007F-<>\"\\s{-}\\^⟨⟩`]+"

[hints.enabled.binding]
key = "U"
mods = "Control|Shift"

[hints.enabled.mouse]
enabled = true
mods = "None"

[mouse]
hide_when_typing = true

[selection]
save_to_clipboard = true

[terminal.shell]
{{- if eq .chezmoi.os "darwin" }}
program = "/opt/homebrew/bin/tmux"
args = [
{{- else if eq .chezmoi.os "linux" -}}
{{-   if eq .chezmoi.osRelease.id "ubuntu" }}
program = "/usr/bin/env"
args = [
  "tmux",
{{-   end }}
{{- end }}
  "new-session",
  "-A",
  "-D",
  "-s",
  "main"
]

[window]
{{- if eq .chezmoi.os "darwin" }}
# SimpleFullscreen covers the screen but stays in the current macOS
# Space — unlike native Fullscreen which takes its own Space and
# breaks cmd+tab flow.
startup_mode = "SimpleFullscreen"
{{- else }}
startup_mode = "Maximized"
{{- end }}
decorations = "none"
opacity = 0.7
# blur only takes effect on macOS / KDE Wayland; harmless no-op elsewhere
blur = true

[window.padding]
x = 6
y = 6
