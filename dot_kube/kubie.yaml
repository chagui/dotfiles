shell: zsh
default_editor: nvim
configs:
  include:
    - ~/.config/kube/config
  exclude:
    - ~/.kube/kubie.yaml

prompt:
  # we use our own p10k segment instead, see hooks
  disable: true

hooks:
  start_ctx: >
    POWERLEVEL9K_LEFT_PROMPT_ELEMENTS=(dir kubie prompt_char)
